{# Auto-generated by OrderFlow Pipeline Factory #}
{# Source: factory/sources/{{ config.source.name }}.yaml #}
version: 2

models:
  - name: stg_{{ config.source.name }}
    description: "Staging view for {{ config.source.name }} (auto-generated)"
    columns:
{% for col in config.columns %}
{% if col.get('pii') %}
      - name: {{ col.name }}_hash
        description: "SHA-256 hash of {{ col.name }} (PII)"
{% elif col.get('is_timestamp') %}
      - name: {{ col.name }}_dt
        description: "{{ col.name }} converted to DateTime"
{% else %}
      - name: {{ col.name }}
        description: "{{ col.name }}"
{% endif %}
{% endfor %}
